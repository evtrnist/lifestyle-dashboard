# lifestyle-data-service

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build lifestyle-data-service` to build the library.

## Running unit tests

Run `nx test lifestyle-data-service` to execute the unit tests via [Jest](https://jestjs.io).
